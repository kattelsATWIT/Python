# By Sank
import math 
#importing Math module to use pi in this specific program. 
r = float(input("Enter R"))
# input here is used to take input from the user and float converts the so string 'r' to floating value
Area = math.pi*r*r
# Area is used to compute the area of a circle
print(str(Area))
#finally the use of 'str' here is used to print the computed area as string.